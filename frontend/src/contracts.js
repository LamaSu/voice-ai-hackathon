/**
 * The wire names from COORDINATION.md, mirrored for the browser.
 *
 * Keep in step with `backend/app/contracts.py`. These are contracts: changing
 * one needs a `contract-change` issue and a heads-up to every lane owner.
 */
export const MSG_USER_STATE = "user_state";
export const MSG_TURN = "turn";
export const MSG_SPEAK = "speak";
export const MSG_METRICS = "metrics";
